# =============================================================================
# MULLBOT - VARIABLES DE ENTORNO
# =============================================================================
# Copia este archivo a .env y configura tus valores reales
# Nunca subas el archivo .env a Git (ya está en .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# ENTORNO Y SERVIDOR
# -----------------------------------------------------------------------------
# Entorno de ejecución: development, production
ENV=development

# Puerto en el que correrá el servidor Express
PORT=3000

# URL base de la API (solo para producción, Railway lo configura automáticamente)
# API_BASE_URL=https://tu-app.up.railway.app

# -----------------------------------------------------------------------------
# INTELIGENCIA ARTIFICIAL (IA)
# -----------------------------------------------------------------------------
# API Key de Google Gemini AI (OBLIGATORIO)
# Obtén tu API key gratuita en: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSyCjN-SiPpKayCQMW70GVIQi3LNsFH7xIDg

# API Key de Claude/Anthropic AI (OPCIONAL - fallback si Gemini falla)
# Obtén tu API key en: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# BASE DE DATOS - MONGODB
# -----------------------------------------------------------------------------
# URI de conexión a MongoDB (OBLIGATORIO)
# Formato: mongodb://[usuario]:[password]@[host]:[puerto]/[database]
# Local: mongodb://localhost:27017/mullbot
# Atlas: mongodb+srv://usuario:password@cluster.mongodb.net/mullbot
MONGODB_URI=mongodb://localhost:27017/mullbot

# Variables alternativas para Railway (se detectan automáticamente):
# MONGO_PRIVATE_URL=mongodb://user:pass@host:27017/mullbot
# MONGO_URL=mongodb://user:pass@host:27017/mullbot
# MONGODB_URL=mongodb://user:pass@host:27017/mullbot

# Variables individuales de MongoDB para Railway (si no usas URI completa):
# MONGO_HOST=localhost
# MONGO_PORT=27017
# MONGO_USER=admin
# MONGO_PASSWORD=password
# MONGO_ROOT_USER=admin
# MONGO_ROOT_PASSWORD=password
# MONGO_DATABASE=mullbot
# RAILWAY_PRIVATE_DOMAIN=mongodb.railway.internal

# -----------------------------------------------------------------------------
# SEGURIDAD
# -----------------------------------------------------------------------------
# Secreto para firmar tokens JWT (OBLIGATORIO)
# Genera uno aleatorio y manténlo seguro
# Ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=cambiar_este_secreto_en_produccion_usar_cadena_aleatoria_larga

# -----------------------------------------------------------------------------
# WHATSAPP - PUPPETEER
# -----------------------------------------------------------------------------
# Ruta al ejecutable de Chrome/Chromium (OPCIONAL - autodetectado)
# Windows: C:\Program Files\Google\Chrome\Application\chrome.exe
# Linux: /usr/bin/google-chrome o /usr/bin/chromium
# macOS: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Railway: Se autodetecta en /usr/bin/google-chrome o /usr/bin/chromium
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome

# -----------------------------------------------------------------------------
# APIS OPCIONALES - FEATURES ADICIONALES
# -----------------------------------------------------------------------------
# Estas APIs son OPCIONALES y solo se necesitan si quieres usar estas features

# API Key de OpenWeatherMap (para comando de clima /meteo)
# Obtén tu API key en: https://openweathermap.org/api
# OPENWEATHERMAP_API_KEY=your_openweather_api_key_here

# API Key de Speechify (para text-to-speech - respuestas de voz)
# Obtén tu API key en: https://speechify.com/api
# SPEECHIFY_API_KEY=your_speechify_api_key_here

# API Key de AssemblyAI (para speech-to-text - notas de voz)
# Obtén tu API key en: https://www.assemblyai.com/
# ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. Variables OBLIGATORIAS:
#    - GEMINI_API_KEY
#    - ENV
#    - PORT
#    - MONGODB_URI (o variables de Railway)
#    - JWT_SECRET
#
# 2. Variables OPCIONALES (features adicionales):
#    - ANTHROPIC_API_KEY (fallback IA)
#    - OPENWEATHERMAP_API_KEY (clima)
#    - SPEECHIFY_API_KEY (voz)
#    - ASSEMBLYAI_API_KEY (audio)
#    - PUPPETEER_EXECUTABLE_PATH (autodetectado)
#
# 3. En Railway.app, configura las variables directamente en el dashboard:
#    Settings > Variables > Add Variable
#
# 4. Para generar un JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 5. Para MongoDB local, asegúrate de tener MongoDB corriendo:
#    mongod --dbpath=/path/to/data
#
# 6. Para MongoDB Atlas (recomendado para producción):
#    - Crea un cluster gratuito en https://mongodb.com/atlas
#    - Obtén tu connection string
#    - Reemplaza <password> y <database> con tus valores
# =============================================================================
