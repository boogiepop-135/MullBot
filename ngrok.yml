version: "2"
# ⚠️ IMPORTANTE: Reemplaza 'TU_NGROK_AUTHTOKEN_AQUI' con tu token real
# Obtén tu token en: https://dashboard.ngrok.com/get-started/your-authtoken
# O usa la variable de entorno NGROK_AUTHTOKEN en docker-compose.yml
authtoken: TU_NGROK_AUTHTOKEN_AQUI
tunnels:
  mullbot:
    addr: app:3001
    proto: http
    schemes:
      - https
    inspect: true
