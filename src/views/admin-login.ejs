<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Müllblue CRM - Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
        }
      }
    }
  </script>

  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #ffffff;
      min-height: 100vh;
    }

    /* Card simple */
    .login-card {
      background: #ffffff;
      border: 2px solid #000000;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    /* Inputs simples */
    .input-simple {
      background: #ffffff;
      border: 2px solid #000000;
      color: #000000;
      transition: all 0.2s ease;
    }

    .input-simple:focus {
      outline: none;
      border-color: #000000;
      box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.1);
    }

    .input-simple::placeholder {
      color: #666666;
    }

    /* Botón simple */
    .btn-simple {
      background: #000000;
      color: #ffffff;
      transition: all 0.2s ease;
    }

    .btn-simple:hover {
      background: #333333;
    }

    .btn-simple:active {
      background: #000000;
    }

    /* Logo simple */
    .logo-container {
      background: #000000;
      border: 2px solid #000000;
    }
  </style>
</head>

<body class="flex items-center justify-center p-4">

  <!-- Contenedor principal -->
  <div class="w-full max-w-md">

    <!-- Card de Login -->
    <div class="login-card rounded-lg p-8 md:p-10">

      <!-- Logo -->
      <div class="flex justify-center mb-6">
        <div class="logo-container w-24 h-24 rounded-lg p-2">
          <div class="w-full h-full bg-white rounded flex items-center justify-center overflow-hidden">
            <img src="/Logo_cara.png" alt="MüllBot" class="w-20 h-20 object-cover">
          </div>
        </div>
      </div>

      <!-- Título -->
      <div class="text-center mb-8">
        <h1 class="text-4xl md:text-5xl font-bold mb-2 text-black">
          Müllblue
        </h1>
        <p class="text-gray-700 text-lg font-medium">
          <i class="fas fa-leaf mr-2"></i>
          Portal de Administración
        </p>
        <div class="mt-4 h-1 w-24 mx-auto bg-black rounded-full"></div>
      </div>

      <!-- Formulario -->
      <form id="login-form" class="space-y-5">

        <!-- Usuario -->
        <div>
          <label class="block text-black font-semibold mb-2 ml-1">
            <i class="fas fa-user mr-2"></i>Usuario
          </label>
          <div class="relative">
            <input
              type="text"
              id="username"
              name="username"
              required
              class="input-simple w-full px-4 py-3.5 rounded-lg focus:outline-none"
              placeholder="Tu nombre de usuario">
          </div>
        </div>

        <!-- Contraseña -->
        <div>
          <label class="block text-black font-semibold mb-2 ml-1">
            <i class="fas fa-lock mr-2"></i>Contraseña
          </label>
          <div class="relative">
            <input
              type="password"
              id="password"
              name="password"
              required
              class="input-simple w-full px-4 py-3.5 rounded-lg focus:outline-none"
              placeholder="Tu contraseña">
          </div>
        </div>

        <!-- Botón -->
        <button
          type="submit"
          class="btn-simple w-full py-4 rounded-lg font-bold text-lg mt-6 focus:outline-none">
          <i class="fas fa-sign-in-alt mr-2"></i>
          Iniciar Sesión
        </button>

        <!-- Error -->
        <div id="error-message" class="hidden mt-4 p-4 bg-white border-2 border-black rounded-lg text-black text-center">
        </div>
      </form>

      <!-- Footer -->
      <div class="mt-8 pt-6 border-t border-gray-300">
        <div class="flex items-center justify-center gap-3 text-gray-700 text-sm">
          <i class="fas fa-shield-alt"></i>
          <span>Sistema Seguro</span>
          <span>•</span>
          <i class="fas fa-leaf"></i>
          <span>Gestión Sostenible</span>
        </div>
        <p class="text-center text-gray-600 text-xs mt-3">
          MüllBot © 2025 - Tecnología Verde
        </p>
      </div>

    </div>
  </div>

  <script>
    document.getElementById('login-form').addEventListener('submit', async function(e) {
      e.preventDefault();

      const username = document.getElementById('username').value;
      const password = document.getElementById('password').value;
      const errorElement = document.getElementById('error-message');
      const submitButton = e.target.querySelector('button[type="submit"]');

      // Loading state
      submitButton.disabled = true;
      submitButton.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Autenticando...';

      try {
        const response = await fetch('/crm/auth/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ username, password })
        });

        if (!response.ok) {
          const error = await response.json();
          throw new Error(error.error || 'Error de autenticación');
        }

        const { token } = await response.json();
        localStorage.setItem('token', token);

        // Success state
        submitButton.innerHTML = '<i class="fas fa-check-circle mr-2"></i>¡Bienvenido!';

        setTimeout(() => {
          window.location.href = '/admin';
        }, 500);

      } catch (error) {
        errorElement.innerHTML = `
          <i class="fas fa-exclamation-triangle mr-2"></i>
          ${error.message}
        `;
        errorElement.classList.remove('hidden');

        // Reset button
        submitButton.disabled = false;
        submitButton.innerHTML = '<i class="fas fa-sign-in-alt mr-2"></i>Iniciar Sesión';

        setTimeout(() => {
          errorElement.classList.add('hidden');
        }, 4000);
      }
    });
  </script>

</body>
</html>
