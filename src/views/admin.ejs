<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Müllblue CRM</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary: #4f46e5;
      --primary-light: #6366f1;
      --sidebar-width: 240px;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background-color: #f8fafc;
    }

    .sidebar {
      width: var(--sidebar-width);
      transition: all 0.3s ease;
    }

    .main-content {
      margin-left: var(--sidebar-width);
      transition: all 0.3s ease;
    }

    .nav-item.active {
      background-color: #f1f5f9;
      border-left: 3px solid var(--primary);
      color: var(--primary);
    }

    .nav-item:hover:not(.active) {
      background-color: #f8fafc;
    }

    .table-row:hover {
      background-color: #f8fafc;
    }

    .badge {
      font-size: 0.75rem;
      padding: 0.25rem 0.5rem;
      border-radius: 0.375rem;
    }

    .badge-sent {
      background-color: #ecfdf5;
      color: #059669;
    }

    .badge-scheduled {
      background-color: #eff6ff;
      color: #2563eb;
    }

    .badge-failed {
      background-color: #fef2f2;
      color: #dc2626;
    }

    .badge-draft {
      background-color: #f5f5f4;
      color: #57534e;
    }

    input:focus,
    textarea:focus {
      box-shadow: 0 0 0 3px rgba(210, 214, 218, 0.3);
    }

    .contact-checkbox:checked+.contact-label {
      background-color: #f1f5f9;
    }

    /* WhatsApp preview styles */
    .whatsapp-preview {
      border-radius: 7.5px;
      box-shadow: 0 1px 0.5px rgba(0, 0, 0, 0.13);
    }

    .whatsapp-message {
      border-radius: 7.5px;
      box-shadow: 0 1px 0.5px rgba(0, 0, 0, 0.1);
      position: relative;
    }

    .whatsapp-message:after {
      content: "";
      position: absolute;
      left: -8px;
      top: 0;
      width: 0;
      height: 0;
      border-top: 10px solid transparent;
      border-bottom: 10px solid transparent;
      border-right: 10px solid #dcf8c6;
    }

    /* Animation for message button */
    .message-btn:hover {
      transform: translateY(-1px);
    }

    /* Modal styles */
    .modal-overlay {
      background-color: rgba(0, 0, 0, 0.5);
      z-index: 1000;
    }

    .modal-content {
      transform: translate(-50%, -50%);
      z-index: 1001;
    }
  </style>
</head>

<body class="min-h-screen flex">
  <!-- Sidebar -->
  <aside class="sidebar fixed h-full bg-white border-r border-gray-200 shadow-sm">
    <div class="p-4 border-b border-gray-200">
      <div class="flex items-center space-x-3">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-gray-700" fill="none" viewBox="0 0 24 24"
          stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
        </svg>
        <h1 class="text-xl font-medium text-gray-800">Müllblue CRM</h1>
      </div>
    </div>
    <nav class="p-4">
      <ul class="space-y-1">
        <li>
          <a href="#" id="dashboard-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-tachometer-alt w-5 text-center"></i>
            <span>Panel</span>
          </a>
        </li>
        <li>
          <a href="#" id="contacts-tab"
            class="nav-item active flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-users w-5 text-center"></i>
            <span>Contactos</span>
          </a>
        </li>
        <li>
          <a href="#" id="campaigns-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-bullhorn w-5 text-center"></i>
            <span>Campañas</span>
          </a>
        </li>
        <li>
          <a href="#" id="new-campaign-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-plus-circle w-5 text-center"></i>
            <span>Nueva Campaña</span>
          </a>
        </li>
        <li>
          <a href="#" id="templates-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-file-alt w-5 text-center"></i>
            <span>Plantillas</span>
          </a>
        </li>
        <li>
          <a href="#" id="settings-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-cog w-5 text-center"></i>
            <span>Configuración</span>
          </a>
        </li>
        <li>
          <a href="#" id="notifications-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-bell w-5 text-center"></i>
            <span>Notificaciones</span>
            <span id="notification-badge" class="hidden bg-red-500 text-white text-xs font-semibold px-1.5 py-0.5 rounded-full ml-auto">0</span>
          </a>
        </li>
        <li>
          <a href="#" id="users-tab"
            class="nav-item flex items-center space-x-3 px-3 py-2.5 rounded-lg text-gray-700 font-medium">
            <i class="fas fa-users w-5 text-center"></i>
            <span>Usuarios</span>
          </a>
        </li>
      </ul>
    </nav>
    <div class="absolute bottom-0 w-full p-4 border-t border-gray-200">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-2">
          <div class="w-8 h-8 rounded-full bg-gray-200 flex items-center justify-center">
            <i class="fas fa-user text-gray-600"></i>
          </div>
          <span id="username" class="text-gray-700 font-medium">Admin</span>
        </div>
        <button id="logout" class="text-gray-500 hover:text-gray-700">
          <i class="fas fa-sign-out-alt"></i>
        </button>
      </div>
    </div>
  </aside>

  <!-- Main Content -->
  <main class="main-content flex-grow">
      <!-- Dashboard Section -->
    <div id="dashboard-section" class="section-content hidden p-6">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
        <!-- Stat 1 - Leads -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Leads</p>
              <h3 id="leads-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Nuevos clientes</p>
            </div>
            <div class="p-3 rounded-full bg-gray-50 text-gray-600">
              <i class="fas fa-user-plus text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Stat 2 - Interesados -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Interesados</p>
              <h3 id="interested-contacts" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Gente interesada</p>
            </div>
            <div class="p-3 rounded-full bg-blue-50 text-blue-600">
              <i class="fas fa-eye text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Stat 3 - Pago Pendiente -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Pago Pendiente</p>
              <h3 id="payment-pending-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Esperan confirmación</p>
            </div>
            <div class="p-3 rounded-full bg-yellow-50 text-yellow-600">
              <i class="fas fa-clock text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Stat 4 - Citas Confirmadas -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Citas Confirmadas</p>
              <h3 id="appointment-confirmed-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Para instalación</p>
            </div>
            <div class="p-3 rounded-full bg-green-50 text-green-600">
              <i class="fas fa-calendar-check text-xl"></i>
            </div>
          </div>
        </div>
      </div>

      <!-- Secondary Stats Row -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
        <!-- Info Solicitada -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Info Solicitada</p>
              <h3 id="info-requested-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Pidieron info</p>
            </div>
            <div class="p-3 rounded-full bg-indigo-50 text-indigo-600">
              <i class="fas fa-info-circle text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Citas Agendadas -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Citas Agendadas</p>
              <h3 id="appointment-scheduled-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Por confirmar</p>
            </div>
            <div class="p-3 rounded-full bg-orange-50 text-orange-600">
              <i class="fas fa-calendar text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Completados -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Completados</p>
              <h3 id="completed-count" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Instalación lista</p>
            </div>
            <div class="p-3 rounded-full bg-green-50 text-green-600">
              <i class="fas fa-check-double text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Pausados -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Pausados</p>
              <h3 id="paused-contacts" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Sin respuestas</p>
            </div>
            <div class="p-3 rounded-full bg-red-50 text-red-600">
              <i class="fas fa-pause text-xl"></i>
            </div>
          </div>
        </div>
      </div>

      <!-- Secondary Stats -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
        <!-- Campaigns -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Campañas</p>
              <h3 id="total-campaigns" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1" id="sent-campaigns-text">0 enviadas</p>
            </div>
            <div class="p-3 rounded-full bg-green-50 text-green-600">
              <i class="fas fa-bullhorn text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Messages Sent -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Mensajes Enviados</p>
              <h3 id="total-messages" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1">Total de mensajes</p>
            </div>
            <div class="p-3 rounded-full bg-purple-50 text-purple-600">
              <i class="fas fa-paper-plane text-xl"></i>
            </div>
          </div>
        </div>

        <!-- Sales Interactions -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-500">Interacciones</p>
              <h3 id="total-interactions" class="text-2xl font-semibold text-gray-800 mt-1">0</h3>
              <p class="text-xs text-gray-400 mt-1" id="unique-users-text">0 usuarios únicos</p>
            </div>
            <div class="p-3 rounded-full bg-yellow-50 text-yellow-600">
              <i class="fas fa-chart-line text-xl"></i>
            </div>
          </div>
        </div>
      </div>

      <!-- Sales Intent Stats -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
        <!-- Intent Breakdown -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
          <div class="p-5 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-800">Intentos de Venta Detectados</h2>
          </div>
          <div class="p-5" id="intent-stats">
            <!-- Will be filled by JavaScript -->
          </div>
        </div>

        <!-- Top Leads -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
          <div class="p-5 border-b border-gray-200">
            <h2 class="text-lg font-medium text-gray-800">Top Leads</h2>
          </div>
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Contacto</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Score</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Interacciones</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Acciones</th>
                </tr>
              </thead>
              <tbody id="top-leads-table" class="bg-white divide-y divide-gray-200">
                <!-- Will be filled by JavaScript -->
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Recent Contacts -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Contactos Recientes</h2>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Nombre</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Teléfono</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Última Interacción</th>
              </tr>
            </thead>
            <tbody id="recent-contacts" class="bg-white divide-y divide-gray-200">
              <!-- Filled by JavaScript -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Contacts Section -->
    <div id="contacts-section" class="section-content p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200">
          <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
            <h2 class="text-lg font-medium text-gray-800">Contactos</h2>
            <div class="flex flex-col md:flex-row gap-3 w-full md:w-auto items-center">
              <!-- WhatsApp Web Button -->
              <button onclick="openWhatsAppWeb()" id="whatsapp-web-btn-contacts" disabled
                class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed text-sm">
                <i class="fas fa-external-link-alt mr-2"></i> WhatsApp Web
              </button>
              <!-- Sale Status Filter -->
              <select id="sale-status-filter" class="border border-gray-200 rounded-lg px-4 py-2 focus:outline-none focus:border-gray-300 transition-colors">
                <option value="">Todos los estados</option>
                <option value="lead">Leads</option>
                <option value="interested">Interesados</option>
                <option value="info_requested">Info Solicitada</option>
                <option value="payment_pending">Pago Pendiente</option>
                <option value="appointment_scheduled">Cita Agendada</option>
                <option value="appointment_confirmed">Cita Confirmada</option>
                <option value="completed">Completados</option>
                <option value="paused">Pausados</option>
              </select>
              <!-- Search -->
              <div class="relative w-full md:w-64">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                  <i class="fas fa-search text-gray-400"></i>
                </div>
                <input type="text" id="contact-search" placeholder="Buscar contactos..."
                  class="pl-10 w-full border border-gray-200 rounded-lg px-4 py-2 focus:outline-none focus:border-gray-300 transition-colors">
              </div>
            </div>
          </div>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Teléfono</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Nombre</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Estado</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Última Interacción</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Pausado</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Acciones</th>
              </tr>
            </thead>
            <tbody id="contacts-table-body" class="bg-white divide-y divide-gray-200">
              <!-- Contacts will be loaded here -->
            </tbody>
          </table>
        </div>
        <div class="px-5 py-3 border-t border-gray-200 flex items-center justify-between">
          <div class="text-sm text-gray-500" id="contacts-pagination-info">
            Mostrando <span id="contacts-start">0</span> a <span id="contacts-end">0</span> de <span
              id="contacts-total">0</span> contactos
          </div>
          <div id="contacts-pagination" class="flex space-x-1">
            <!-- Pagination will be loaded here -->
          </div>
        </div>
      </div>
    </div>

    <!-- Campaigns Section -->
    <div id="campaigns-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Campañas</h2>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Nombre</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Estado</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Programada</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Enviados</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Fallidos</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                  Acciones</th>
              </tr>
            </thead>
            <tbody id="campaigns-table-body" class="bg-white divide-y divide-gray-200">
              <!-- Campaigns will be loaded here -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- New Campaign Section -->
    <div id="new-campaign-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Crear Nueva Campaña</h2>
        </div>
        <form id="campaign-form" class="p-5">
          <div class="space-y-5">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Nombre de la Campaña</label>
              <input type="text" name="name" required
                class="w-full border border-gray-200 rounded-lg px-4 py-2.5 focus:outline-none focus:border-gray-300 transition-colors">
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Mensaje</label>
                <textarea name="message" id="campaign-message" rows="8" required
                  class="w-full border border-gray-200 rounded-lg px-4 py-2.5 focus:outline-none focus:border-gray-300 transition-colors"
                  oninput="updateWhatsAppPreview()"></textarea>

                <div class="mt-4">
                  <label class="block text-sm font-medium text-gray-700 mb-1">Variables Disponibles</label>
                  <div class="flex flex-wrap gap-2">
                    <button type="button" onclick="insertVariable('{name}')"
                      class="text-xs bg-gray-100 hover:bg-gray-200 px-2 py-1 rounded">
                      {name}
                    </button>
                    <button type="button" onclick="insertVariable('{phone}')"
                      class="text-xs bg-gray-100 hover:bg-gray-200 px-2 py-1 rounded">
                      {phone}
                    </button>
                    <button type="button" onclick="insertVariable('{date}')"
                      class="text-xs bg-gray-100 hover:bg-gray-200 px-2 py-1 rounded">
                      {date}
                    </button>
                  </div>
                </div>
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">WhatsApp Preview</label>
                <div class="whatsapp-preview-container bg-gray-50 p-4 rounded-lg">
                  <div class="whatsapp-preview bg-white p-4 rounded-lg shadow-sm max-w-xs mx-auto">
                    <div class="flex items-center mb-3">
                      <div class="w-8 h-8 rounded-full bg-green-500 flex items-center justify-center text-white mr-2">
                        <i class="fas fa-user"></i>
                      </div>
                      <div class="text-sm font-medium">You</div>
                    </div>
                    <div class="whatsapp-message bg-green-100 p-3 rounded-lg text-sm">
                      <!-- Message will appear here -->
                    </div>
                    <div class="text-xs text-gray-500 mt-1 text-right">Now</div>
                  </div>
                </div>
              </div>
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Schedule (leave empty for immediate
                send)</label>
              <input type="datetime-local" name="scheduledAt"
                class="border border-gray-200 rounded-lg px-4 py-2.5 focus:outline-none focus:border-gray-300 transition-colors">
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">Select Contacts</label>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <div class="border border-gray-200 rounded-lg p-3 h-96 overflow-y-auto">
                    <h3 class="font-medium text-gray-700 mb-3">Available Contacts</h3>
                    <div id="available-contacts" class="space-y-2">
                      <!-- Contacts will be loaded here -->
                    </div>
                  </div>
                </div>
                <div>
                  <div class="border border-gray-200 rounded-lg p-3 h-96 overflow-y-auto" id="selected-contacts">
                    <h3 class="font-medium text-gray-700 mb-3">Selected Contacts</h3>
                    <p class="text-gray-400 text-sm" id="no-contacts-message">No contacts selected</p>
                  </div>
                </div>
              </div>
            </div>

            <div class="pt-2">
              <button type="submit"
                class="bg-gray-800 hover:bg-gray-700 text-white px-5 py-2.5 rounded-lg font-medium transition-colors">
                <i class="fas fa-paper-plane mr-2"></i> Send Campaign
              </button>
            </div>
          </div>
        </form>
      </div>
    </div>

    <!-- Templates Section -->
    <div id="templates-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200 flex justify-between items-center">
          <h2 class="text-lg font-medium text-gray-800">Plantillas de Mensajes</h2>
          <button onclick="openNewTemplateModal()"
            class="bg-gray-800 hover:bg-gray-700 text-white px-4 py-2 rounded-lg text-sm">
            <i class="fas fa-plus mr-1"></i> Nueva Plantilla
          </button>
        </div>

        <div class="p-5">
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4" id="templates-container">
            <!-- Templates will be loaded here -->
          </div>
        </div>
      </div>
    </div>

    <!-- Settings Section -->
    <div id="settings-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Configuración del Bot</h2>
        </div>
        <div class="p-5 space-y-6">
          <!-- Bot Delay Configuration -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Delay del Bot (milisegundos)</label>
            <div class="flex items-center space-x-4">
              <input type="number" id="bot-delay-input" min="0" step="1000"
                class="border border-gray-300 rounded-lg px-4 py-2 focus:ring-blue-500 focus:border-blue-500 w-32">
              <button onclick="saveBotConfig()"
                class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium">
                Guardar
              </button>
              <span class="text-sm text-gray-500" id="current-delay-text">Cargando...</span>
            </div>
            <p class="text-xs text-gray-400 mt-2">Tiempo de espera antes de responder mensajes (por defecto: 10000ms = 10 segundos)</p>
          </div>
        </div>
      </div>

      <!-- Change Password Section -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Cambiar Contraseña</h2>
        </div>
        <div class="p-5 space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Contraseña Actual</label>
            <input type="password" id="current-password-input"
              class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-blue-500 focus:border-blue-500">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Nueva Contraseña</label>
            <input type="password" id="new-password-input"
              class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-blue-500 focus:border-blue-500">
            <p class="text-xs text-gray-400 mt-1">La contraseña debe tener al menos 6 caracteres</p>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Confirmar Nueva Contraseña</label>
            <input type="password" id="confirm-password-input"
              class="w-full border border-gray-300 rounded-lg px-4 py-2 focus:ring-blue-500 focus:border-blue-500">
          </div>
          <button onclick="changePassword()"
            class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2.5 rounded-lg font-medium transition-colors">
            <i class="fas fa-key mr-2"></i> Cambiar Contraseña
          </button>
        </div>
      </div>

      <!-- WhatsApp Connection Section -->
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200">
          <h2 class="text-lg font-medium text-gray-800">Conexión de WhatsApp</h2>
        </div>
        <div class="p-5 space-y-4">
          <div>
            <p class="text-sm text-gray-700 mb-4">Desvincular la cuenta actual de WhatsApp y vincular una nueva.</p>
            <div class="flex flex-col sm:flex-row gap-3">
              <button onclick="logoutWhatsApp()"
                class="bg-red-600 hover:bg-red-700 text-white px-5 py-2.5 rounded-lg font-medium transition-colors">
                <i class="fas fa-unlink mr-2"></i> Desvincular WhatsApp
              </button>
              <button onclick="openWhatsAppWeb()" id="whatsapp-web-btn" disabled
                class="bg-green-600 hover:bg-green-700 text-white px-5 py-2.5 rounded-lg font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed">
                <i class="fas fa-external-link-alt mr-2"></i> Abrir WhatsApp Web
              </button>
            </div>
            <p class="text-xs text-gray-400 mt-2">Después de desvincular, se generará un nuevo código QR para escanear con otra cuenta.</p>
          </div>
          <div id="whatsapp-status" class="mt-4 p-4 bg-gray-50 rounded-lg">
            <p class="text-sm text-gray-600">
              <span id="whatsapp-connected-status">Verificando estado...</span>
            </p>
            <div id="qr-display" class="mt-4 hidden">
              <p class="text-sm font-medium text-gray-700 mb-2">Escanear QR para vincular WhatsApp:</p>
              <div id="qr-code-container" class="bg-white p-4 rounded-lg border border-gray-200"></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Users Management Section -->
    <div id="users-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">
        <div class="p-5 border-b border-gray-200 flex justify-between items-center">
          <h2 class="text-lg font-medium text-gray-800">Gestión de Usuarios</h2>
          <button onclick="openCreateUserModal()"
            class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium">
            <i class="fas fa-plus mr-2"></i> Crear Usuario
          </button>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Usuario</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Rol</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Creado</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
              </tr>
            </thead>
            <tbody id="users-table-body" class="bg-white divide-y divide-gray-200">
              <!-- Users will be loaded here -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Notifications Section -->
    <div id="notifications-section" class="section-content hidden p-6">
      <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
        <div class="p-5 border-b border-gray-200 flex justify-between items-center">
          <h2 class="text-lg font-medium text-gray-800">Notificaciones</h2>
          <div class="flex items-center space-x-3">
            <span id="unread-count-badge" class="bg-red-500 text-white text-xs font-semibold px-2 py-1 rounded-full">0</span>
            <button onclick="markAllNotificationsAsRead()"
              class="text-sm text-blue-600 hover:text-blue-800">
              Marcar todas como leídas
            </button>
          </div>
        </div>
        <div class="p-5">
          <div id="notifications-list" class="space-y-3">
            <!-- Notifications will be loaded here -->
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- WhatsApp Chat Modal -->
  <div id="chat-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
      <div class="fixed inset-0 transition-opacity" aria-hidden="true">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
      </div>
      <div
        class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-2xl sm:w-full h-[80vh] flex flex-col">
        <!-- Chat Header -->
        <div class="bg-green-600 px-4 py-4 sm:px-6 flex items-center justify-between">
          <div class="flex items-center space-x-3">
            <div class="w-10 h-10 rounded-full bg-green-700 flex items-center justify-center">
              <i class="fas fa-user text-white"></i>
            </div>
            <div>
              <h3 id="chat-contact-name" class="text-lg font-medium text-white">Contacto</h3>
              <p id="chat-contact-phone" class="text-sm text-green-100"></p>
            </div>
          </div>
          <div class="flex items-center space-x-2">
            <button type="button" onclick="openWhatsAppWebForContact()" id="whatsapp-web-contact-btn" disabled
              class="bg-green-700 hover:bg-green-800 text-white px-3 py-2 rounded-lg text-sm font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
              title="Abrir WhatsApp Web para este contacto">
              <i class="fas fa-external-link-alt mr-1"></i> WhatsApp Web
            </button>
            <button type="button" onclick="closeChatModal()"
              class="text-white hover:text-gray-200">
              <i class="fas fa-times text-xl"></i>
            </button>
          </div>
        </div>

        <!-- Messages Container (WhatsApp style) -->
        <div id="messages-container" class="flex-1 overflow-y-auto bg-gray-100 p-4 space-y-2">
          <!-- Messages will be loaded here -->
        </div>

        <!-- Message Input -->
        <div class="bg-white border-t border-gray-200 px-4 py-3">
          <div class="flex items-center space-x-2">
            <button type="button" class="text-gray-400 hover:text-gray-600">
              <i class="fas fa-paperclip"></i>
            </button>
            <textarea id="chat-message-input" rows="1"
              class="flex-1 border border-gray-300 rounded-lg px-4 py-2 focus:ring-green-500 focus:border-green-500 resize-none"
              placeholder="Escribe un mensaje..."
              onkeydown="if(event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); sendChatMessage(); }"></textarea>
            <button type="button" onclick="sendChatMessage()"
              class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg">
              <i class="fas fa-paper-plane"></i>
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Status Change Modal -->
  <div id="status-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
      <div class="fixed inset-0 transition-opacity" aria-hidden="true">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
      </div>
      <div
        class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
        <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">Cambiar Estado de Venta</h3>
          <div class="mt-2 space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Estado</label>
              <select id="status-select"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
                <option value="lead">Lead</option>
                <option value="interested">Interesado</option>
                <option value="info_requested">Info Solicitada</option>
                <option value="payment_pending">Pago Pendiente</option>
                <option value="appointment_scheduled">Cita Agendada</option>
                <option value="appointment_confirmed">Cita Confirmada</option>
                <option value="completed">Completado</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Fecha de Cita (si aplica)</label>
              <input type="datetime-local" id="appointment-date"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Notas (opcional)</label>
              <textarea id="status-notes" rows="3"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500"
                placeholder="Agregar notas sobre el cambio de estado..."></textarea>
            </div>
          </div>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
          <button type="button" onclick="saveStatusChange()"
            class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-blue-600 text-base font-medium text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 sm:ml-3 sm:w-auto sm:text-sm">
            Guardar
          </button>
          <button type="button" onclick="closeStatusModal()"
            class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 sm:mt-0 sm:ml-3 sm:w-auto sm:text-sm">
            Cancelar
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Create User Modal -->
  <div id="create-user-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
      <div class="fixed inset-0 transition-opacity" aria-hidden="true">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
      </div>
      <div
        class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
        <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">Crear Usuario</h3>
          <div class="mt-2 space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Nombre de Usuario</label>
              <input type="text" id="create-username-input"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Contraseña</label>
              <input type="password" id="create-password-input"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
              <p class="text-xs text-gray-400 mt-1">Mínimo 6 caracteres</p>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Rol</label>
              <select id="create-role-select"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
                <option value="user">Usuario</option>
                <option value="admin">Administrador</option>
              </select>
            </div>
          </div>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
          <button type="button" onclick="saveCreateUser()"
            class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-blue-600 text-base font-medium text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 sm:ml-3 sm:w-auto sm:text-sm">
            Crear
          </button>
          <button type="button" onclick="closeCreateUserModal()"
            class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 sm:mt-0 sm:ml-3 sm:w-auto sm:text-sm">
            Cancelar
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Change User Password Modal -->
  <div id="change-user-password-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
      <div class="fixed inset-0 transition-opacity" aria-hidden="true">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
      </div>
      <div
        class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
        <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4">Cambiar Contraseña</h3>
          <div class="mt-2 space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Usuario</label>
              <input type="text" id="change-password-username-input" readonly
                class="w-full border border-gray-300 rounded-md p-2 bg-gray-50 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Nueva Contraseña</label>
              <input type="password" id="change-password-new-input"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
              <p class="text-xs text-gray-400 mt-1">Mínimo 6 caracteres</p>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Confirmar Nueva Contraseña</label>
              <input type="password" id="change-password-confirm-input"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
          </div>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
          <button type="button" onclick="saveChangeUserPassword()"
            class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-blue-600 text-base font-medium text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 sm:ml-3 sm:w-auto sm:text-sm">
            Cambiar
          </button>
          <button type="button" onclick="closeChangeUserPasswordModal()"
            class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 sm:mt-0 sm:ml-3 sm:w-auto sm:text-sm">
            Cancelar
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Template Modal -->
  <div id="template-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
      <div class="fixed inset-0 transition-opacity" aria-hidden="true">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
      </div>
      <div
        class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
        <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
          <h3 class="text-lg leading-6 font-medium text-gray-900 mb-4" id="template-modal-title">Nueva Plantilla</h3>
          <div class="mt-2 space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Nombre de la Plantilla</label>
              <input type="text" id="template-name"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Contenido</label>
              <textarea id="template-content" rows="6"
                class="w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500"></textarea>
            </div>
          </div>
        </div>
        <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
          <button type="button" onclick="saveTemplate()"
            class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-blue-600 text-base font-medium text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 sm:ml-3 sm:w-auto sm:text-sm">
            Guardar
          </button>
          <button type="button" onclick="closeTemplateModal()"
            class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 sm:mt-0 sm:ml-3 sm:w-auto sm:text-sm">
            Cancelar
          </button>
        </div>
      </div>
    </div>
  </div>

  <script src="/public/js/admin.js"></script>
  <script>
    // WhatsApp preview functions
    function updateWhatsAppPreview() {
      const message = document.getElementById('campaign-message').value;
      document.querySelector('.whatsapp-message').textContent = message;
    }

    function insertVariable(variable) {
      const textarea = document.getElementById('campaign-message');
      const start = textarea.selectionStart;
      const end = textarea.selectionEnd;
      const text = textarea.value;

      textarea.value = text.substring(0, start) + variable + text.substring(end);
      textarea.selectionStart = textarea.selectionEnd = start + variable.length;
      textarea.focus();

      updateWhatsAppPreview();
    }

    // Message modal functions - DEPRECATED: usar openChatModal en su lugar
    let currentRecipient = null;

    function openMessageModal(phoneNumber, contactName) {
      // Redirigir a openChatModal que es la función correcta
      if (typeof openChatModal === 'function') {
        openChatModal(phoneNumber, contactName || phoneNumber);
      } else {
        console.error('openChatModal no está disponible');
      }
    }

    function closeMessageModal() {
      // Redirigir a closeChatModal
      if (typeof closeChatModal === 'function') {
        closeChatModal();
      }
    }

    async function sendPrivateMessage() {
      const message = document.getElementById('message-content').value;
      if (!message || !currentRecipient) return;

      try {
        const response = await fetch('/crm/send-message', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${localStorage.getItem('token')}`,
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({
            phoneNumber: currentRecipient,
            message
          })
        });

        if (!response.ok) throw new Error('Failed to send message');

        closeMessageModal();
        document.getElementById('message-content').value = '';
        alert('Message sent successfully!');
      } catch (error) {
        console.error('Error sending message:', error);
        alert('Failed to send message');
      }
    }

    // Template modal functions
    let currentTemplateId = null;

    function openNewTemplateModal() {
      currentTemplateId = null;
      document.getElementById('template-modal-title').textContent = 'Nueva Plantilla';
      document.getElementById('template-name').value = '';
      document.getElementById('template-content').value = '';
      document.getElementById('template-modal').classList.remove('hidden');
    }

    function openEditTemplateModal(id, name, content) {
      currentTemplateId = id;
      document.getElementById('template-modal-title').textContent = 'Editar Plantilla';
      document.getElementById('template-name').value = name;
      document.getElementById('template-content').value = content;
      document.getElementById('template-modal').classList.remove('hidden');
    }

    function closeTemplateModal() {
      document.getElementById('template-modal').classList.add('hidden');
      currentTemplateId = null;
    }

    async function saveTemplate() {
      const name = document.getElementById('template-name').value;
      const content = document.getElementById('template-content').value;

      if (!name || !content) {
        alert('Por favor completa todos los campos');
        return;
      }

      try {
        const url = currentTemplateId
          ? `/crm/templates/${currentTemplateId}`
          : '/crm/templates';

        const method = currentTemplateId ? 'PUT' : 'POST';

        const response = await fetch(url, {
          method,
          headers: {
            'Authorization': `Bearer ${localStorage.getItem('token')}`,
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ name, content })
        });

        if (!response.ok) throw new Error('Failed to save template');

        closeTemplateModal();
        loadTemplates();
        alert('Plantilla guardada correctamente');
      } catch (error) {
        console.error('Error saving template:', error);
        alert('Error al guardar la plantilla');
      }
    }

    async function deleteTemplate(id) {
      if (!confirm('¿Estás seguro de que quieres eliminar esta plantilla?')) return;

      try {
        const response = await fetch(`/crm/templates/${id}`, {
          method: 'DELETE',
          headers: {
            'Authorization': `Bearer ${localStorage.getItem('token')}`
          }
        });

        if (!response.ok) throw new Error('Failed to delete template');

        loadTemplates();
        alert('Plantilla eliminada correctamente');
      } catch (error) {
        console.error('Error deleting template:', error);
        alert('Error al eliminar la plantilla');
      }
    }

    function useTemplate(id) {
      // Find template in the list and insert into message field
      const template = Array.from(document.querySelectorAll('#templates-container > div'))
        .find(el => el.dataset.templateId === id);

      if (template) {
        const name = template.querySelector('h3').textContent;
        const content = template.querySelector('p').textContent;

        document.getElementById('campaign-message').value = content;
        updateWhatsAppPreview();

        // Switch to new campaign tab if not already there
        if (document.getElementById('new-campaign-section').classList.contains('hidden')) {
          document.getElementById('new-campaign-tab').click();
        }
      }
    }

    // Initialize preview
    document.addEventListener('DOMContentLoaded', updateWhatsAppPreview);
  </script>
</body>

</html>