<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MüllBot CRM</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
        }
      }
    }
  </script>

  <style>
    * {
      font-family: 'Inter', sans-serif;
    }

    :root {
      --primary: #10b981;
      --primary-dark: #059669;
      --sidebar-width: 280px;
    }

    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
    }

    /* Sidebar glassmorphism */
    .sidebar {
      width: var(--sidebar-width);
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-right: 1px solid rgba(16, 185, 129, 0.1);
      box-shadow: 4px 0 24px rgba(0, 0, 0, 0.06);
      transition: all 0.3s ease;
    }

    .nav-item {
      position: relative;
      transition: all 0.3s ease;
    }

    .nav-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 3px;
      background: linear-gradient(135deg, #10b981, #34d399);
      transform: scaleY(0);
      transition: transform 0.3s ease;
    }

    .nav-item.active::before {
      transform: scaleY(1);
    }

    .nav-item.active {
      background: linear-gradient(90deg, rgba(16, 185, 129, 0.1) 0%, transparent 100%);
      color: #059669;
    }

    .nav-item:hover {
      background: rgba(16, 185, 129, 0.05);
      transform: translateX(2px);
    }

    /* Main content */
    .main-content {
      margin-left: var(--sidebar-width);
      min-height: 100vh;
    }

    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 10px;
      height: 10px;
    }

    ::-webkit-scrollbar-track {
      background: rgba(0, 0, 0, 0.05);
    }

    ::-webkit-scrollbar-thumb {
      background: rgba(16, 185, 129, 0.3);
      border-radius: 5px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: rgba(16, 185, 129, 0.5);
    }

    /* Stats cards with gradient */
    .stat-card {
      background: white;
      border-radius: 16px;
      padding: 20px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
      border: 1px solid rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
    }

    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    }

    .stat-icon {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-center;
      font-size: 20px;
      background: linear-gradient(135deg, #10b981 0%, #34d399 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

    /* Badge styles */
    .badge {
      font-size: 0.75rem;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }

    .badge-sent {
      background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
      color: #065f46;
    }

    .badge-scheduled {
      background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
      color: #1e40af;
    }

    .badge-failed {
      background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
      color: #991b1b;
    }

    .badge-draft {
      background: linear-gradient(135deg, #f3f4f6 0%, #e5e7eb 100%);
      color: #1f2937;
    }

    /* Modern table */
    table {
      border-collapse: separate;
      border-spacing: 0;
    }

    table thead {
      background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
    }

    table tbody tr {
      transition: all 0.2s ease;
    }

    table tbody tr:hover {
      background: rgba(16, 185, 129, 0.02);
      transform: scale(1.01);
    }

    /* Modern buttons */
    .btn-primary {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      padding: 10px 20px;
      border-radius: 12px;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
      border: none;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
    }

    .btn-secondary {
      background: white;
      color: #059669;
      padding: 10px 20px;
      border-radius: 12px;
      font-weight: 600;
      transition: all 0.3s ease;
      border: 2px solid #10b981;
    }

    .btn-secondary:hover {
      background: rgba(16, 185, 129, 0.1);
    }

    /* WhatsApp message */
    .whatsapp-message {
      position: relative;
      background: linear-gradient(135deg, #dcf8c6 0%, #c8e6c9 100%);
      padding: 12px 18px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .whatsapp-message::after {
      content: "";
      position: absolute;
      left: -8px;
      top: 10px;
      width: 0;
      height: 0;
      border-top: 8px solid transparent;
      border-bottom: 8px solid transparent;
      border-right: 8px solid #dcf8c6;
    }

    /* Card container */
    .content-card {
      background: white;
      border-radius: 20px;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
      border: 1px solid rgba(0, 0, 0, 0.05);
      overflow: hidden;
    }

    /* Input modern */
    input:focus, textarea:focus, select:focus {
      outline: none;
      border-color: #10b981;
      box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
    }

    /* Logo glow */
    .logo-glow {
      box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
      transition: all 0.3s ease;
    }

    .logo-glow:hover {
      box-shadow: 0 0 30px rgba(16, 185, 129, 0.5);
      transform: scale(1.05);
    }

    /* Page header */
    .page-header {
      background: white;
      border-radius: 20px;
      padding: 24px;
      margin-bottom: 24px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.06);
    }

    /* Animation */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .section-content {
      animation: fadeIn 0.4s ease-out;
    }
  </style>
</head>

<body class="text-gray-800">

  <!-- Sidebar -->
  <aside class="sidebar fixed inset-y-0 left-0 z-30 flex flex-col">
    <!-- Logo -->
    <div class="h-20 flex items-center px-6 border-b border-gray-100">
      <div class="w-12 h-12 rounded-2xl flex items-center justify-center mr-3 overflow-hidden logo-glow">
        <img src="/public/Logo_cara.png" alt="MüllBot Logo" class="w-full h-full object-cover">
      </div>
      <div>
        <span class="text-xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">MüllBot</span>
        <p class="text-xs text-gray-500">CRM Dashboard</p>
      </div>
    </div>

    <!-- Navigation -->
    <nav class="flex-1 overflow-y-auto py-6 px-4 space-y-2">
      <a href="#" onclick="showSection('dashboard')" id="nav-dashboard"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-home w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Dashboard</span>
      </a>

      <a href="#" onclick="showSection('contacts')" id="nav-contacts"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-address-book w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Contactos</span>
      </a>

      <a href="#" onclick="showSection('campaigns')" id="nav-campaigns"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-bullhorn w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Campañas</span>
      </a>

      <a href="#" onclick="showSection('templates')" id="nav-templates"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-file-alt w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Plantillas</span>
      </a>

      <a href="#" onclick="showSection('products')" id="nav-products"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-box w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Productos</span>
      </a>

      <a href="#" onclick="showSection('users')" id="nav-users"
        class="nav-item hidden flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-users w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Usuarios</span>
      </a>

      <a href="#" onclick="showSection('bot-content')" id="nav-bot-content"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-comment-dots w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Contenido Bot</span>
      </a>

      <a href="#" onclick="showSection('settings')" id="nav-settings"
        class="nav-item flex items-center px-4 py-3.5 text-gray-600 rounded-xl font-medium group">
        <i class="fas fa-cog w-7 text-center text-gray-400 group-hover:text-green-500 transition-colors text-lg"></i>
        <span class="ml-3">Configuración</span>
      </a>
    </nav>

    <!-- User Info & Logout -->
    <div class="border-t border-gray-100 p-4 bg-gradient-to-r from-green-50 to-emerald-50">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <div class="w-10 h-10 rounded-full bg-gradient-to-br from-green-400 to-emerald-500 flex items-center justify-center text-white font-bold text-sm shadow-lg" id="user-initials">?</div>
          <div class="ml-3">
            <p class="text-sm font-semibold text-gray-800" id="user-name">Usuario</p>
            <p class="text-xs text-gray-500" id="user-role">-</p>
          </div>
        </div>
        <button onclick="logout()" class="text-gray-400 hover:text-red-500 transition-colors p-2 hover:bg-red-50 rounded-lg" title="Cerrar sesión">
          <i class="fas fa-sign-out-alt text-lg"></i>
        </button>
      </div>
    </div>
  </aside>

  <!-- Main Content -->
  <main class="main-content min-h-screen">
    <!-- Dashboard Section -->
    <div id="dashboard-section" class="section-content p-8">
      <div class="page-header">
        <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Dashboard</h1>
        <p class="text-gray-600 mt-1">Vista general de tu CRM</p>
      </div>

      <!-- Stats Grid -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-8">
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Leads</p>
              <p class="text-3xl font-bold text-gray-800" id="leads-count">0</p>
            </div>
            <div class="stat-icon">
              <i class="fas fa-user-plus"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Interesados</p>
              <p class="text-3xl font-bold text-gray-800" id="interested-contacts">0</p>
            </div>
            <div class="stat-icon" style="background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);">
              <i class="fas fa-star"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Info Solicitada</p>
              <p class="text-3xl font-bold text-gray-800" id="info-requested-count">0</p>
            </div>
            <div class="stat-icon" style="background: linear-gradient(135deg, #6366f1 0%, #818cf8 100%);">
              <i class="fas fa-info-circle"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Pago Pendiente</p>
              <p class="text-3xl font-bold text-gray-800" id="payment-pending-count">0</p>
            </div>
            <div class="stat-icon" style="background: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);">
              <i class="fas fa-clock"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Cita Agendada</p>
              <p class="text-3xl font-bold text-gray-800" id="appointment-scheduled-count">0</p>
            </div>
            <div class="stat-icon" style="background: linear-gradient(135deg, #f97316 0%, #fb923c 100%);">
              <i class="fas fa-calendar"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Cita Confirmada</p>
              <p class="text-3xl font-bold text-gray-800" id="appointment-confirmed-count">0</p>
            </div>
            <div class="stat-icon">
              <i class="fas fa-calendar-check"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Completados</p>
              <p class="text-3xl font-bold text-gray-800" id="completed-count">0</p>
            </div>
            <div class="stat-icon">
              <i class="fas fa-check-circle"></i>
            </div>
          </div>
        </div>
        <div class="stat-card">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm text-gray-500 font-medium mb-1">Pausados</p>
              <p class="text-3xl font-bold text-gray-800" id="paused-contacts">0</p>
            </div>
            <div class="stat-icon" style="background: linear-gradient(135deg, #ef4444 0%, #f87171 100%);">
              <i class="fas fa-pause"></i>
            </div>
          </div>
        </div>
      </div>

      <!-- Secondary Stats -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="content-card p-6">
          <p class="text-sm text-gray-500 font-medium mb-2">Total Campañas</p>
          <p class="text-2xl font-bold text-gray-800" id="total-campaigns">0</p>
        </div>
        <div class="content-card p-6">
          <p class="text-sm text-gray-500 font-medium mb-2">Mensajes Enviados</p>
          <p class="text-2xl font-bold text-gray-800" id="total-messages">0</p>
        </div>
        <div class="content-card p-6">
          <p class="text-sm text-gray-500 font-medium mb-2">Total Interacciones</p>
          <p class="text-2xl font-bold text-gray-800" id="total-interactions">0</p>
        </div>
      </div>

      <!-- Intent Stats & Top Leads -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="content-card">
          <div class="p-6 border-b border-gray-100">
            <h3 class="font-bold text-lg text-gray-800">Intenciones Detectadas</h3>
          </div>
          <div class="p-6" id="intent-stats">
            <p class="text-gray-400 text-sm text-center py-8">Cargando...</p>
          </div>
        </div>
        <div class="content-card">
          <div class="p-6 border-b border-gray-100">
            <h3 class="font-bold text-lg text-gray-800">Top Leads</h3>
          </div>
          <div class="overflow-x-auto">
            <table class="min-w-full">
              <thead>
                <tr>
                  <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Contacto</th>
                  <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Score</th>
                  <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Acción</th>
                </tr>
              </thead>
              <tbody id="top-leads-table" class="divide-y divide-gray-100">
                <tr><td colspan="3" class="px-6 py-4 text-sm text-gray-500 text-center">Cargando...</td></tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Recent Contacts -->
      <div class="mt-6 content-card">
        <div class="p-6 border-b border-gray-100">
          <h3 class="font-bold text-lg text-gray-800">Contactos Recientes</h3>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full">
            <thead>
              <tr>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Nombre</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Teléfono</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Última Interacción</th>
              </tr>
            </thead>
            <tbody id="recent-contacts" class="divide-y divide-gray-100">
              <tr><td colspan="3" class="px-6 py-4 text-sm text-gray-500 text-center">Cargando...</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Contacts Section -->
    <div id="contacts-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Contactos</h1>
          <p class="text-gray-600 mt-1">Gestiona tus contactos</p>
        </div>
      </div>

      <!-- Filters -->
      <div class="content-card p-6 mb-6">
        <div class="flex flex-wrap gap-4">
          <input type="text" id="contact-search" placeholder="Buscar contactos..."
            class="border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none w-64 transition-all">
          <select id="sale-status-filter" class="border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
            <option value="">Todos los estados</option>
            <option value="lead">Lead</option>
            <option value="interested">Interesado</option>
            <option value="info_requested">Info Solicitada</option>
            <option value="payment_pending">Pago Pendiente</option>
            <option value="appointment_scheduled">Cita Agendada</option>
            <option value="appointment_confirmed">Cita Confirmada</option>
            <option value="completed">Completado</option>
            <option value="paused">Pausados</option>
          </select>
        </div>
      </div>

      <!-- Contacts Table -->
      <div class="content-card">
        <div class="overflow-x-auto">
          <table class="min-w-full">
            <thead>
              <tr>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Teléfono</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Nombre</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Estado</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Última Interacción</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Acciones</th>
              </tr>
            </thead>
            <tbody id="contacts-table-body" class="divide-y divide-gray-100">
              <!-- Contacts loaded here -->
            </tbody>
          </table>
        </div>
        <div class="px-6 py-4 border-t border-gray-100 flex justify-between items-center bg-gray-50">
          <span class="text-sm text-gray-600 font-medium" id="contacts-pagination-info">-</span>
          <div id="contacts-pagination" class="flex space-x-2"></div>
        </div>
      </div>
    </div>

    <!-- Campaigns Section -->
    <div id="campaigns-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Campañas</h1>
          <p class="text-gray-600 mt-1">Gestiona tus campañas de mensajes</p>
        </div>
        <button onclick="showSection('new-campaign')" class="btn-primary">
          <i class="fas fa-plus mr-2"></i>Nueva Campaña
        </button>
      </div>

      <div class="content-card">
        <div class="overflow-x-auto">
          <table class="min-w-full">
            <thead>
              <tr>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Nombre</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Estado</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Programada</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Enviados</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Fallidos</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Acciones</th>
              </tr>
            </thead>
            <tbody id="campaigns-table-body" class="divide-y divide-gray-100">
              <!-- Campaigns loaded here -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- New Campaign Section -->
    <div id="new-campaign-section" class="section-content hidden p-8">
      <div class="page-header">
        <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Nueva Campaña</h1>
        <p class="text-gray-600 mt-1">Crea una nueva campaña de mensajes</p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="content-card p-6">
          <form id="campaign-form">
            <div class="space-y-5">
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Nombre de la Campaña</label>
                <input type="text" name="name" required
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Mensaje</label>
                <textarea name="message" id="campaign-message" rows="5" required
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all"
                  oninput="updateWhatsAppPreview()"></textarea>
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Programar envío (opcional)</label>
                <input type="datetime-local" name="scheduledAt"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <button type="submit" class="btn-primary w-full py-3">
                Crear Campaña
              </button>
            </div>
          </form>
        </div>

        <div class="space-y-6">
          <!-- Contact Selection -->
          <div class="content-card">
            <div class="p-4 border-b border-gray-100">
              <h3 class="font-bold text-gray-800">Seleccionar Contactos</h3>
            </div>
            <div class="p-4 max-h-64 overflow-y-auto" id="available-contacts">
              <!-- Contacts loaded here -->
            </div>
          </div>

          <!-- Selected Contacts -->
          <div class="content-card">
            <div class="p-4 border-b border-gray-100">
              <h3 class="font-bold text-gray-800">Contactos Seleccionados</h3>
            </div>
            <div class="p-4 max-h-40 overflow-y-auto" id="selected-contacts">
              <p class="text-gray-400 text-sm text-center mt-4" id="no-contacts-message">Ningún contacto seleccionado</p>
            </div>
          </div>

          <!-- Preview -->
          <div class="content-card">
            <div class="p-4 border-b border-gray-100">
              <h3 class="font-bold text-gray-800">Vista Previa</h3>
            </div>
            <div class="p-6 bg-gradient-to-br from-gray-50 to-gray-100">
              <div class="whatsapp-message">El mensaje aparecerá aquí...</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Templates Section -->
    <div id="templates-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Plantillas</h1>
          <p class="text-gray-600 mt-1">Gestiona tus plantillas de mensajes</p>
        </div>
        <button onclick="openCreateTemplateModal()" class="btn-primary">
          <i class="fas fa-plus mr-2"></i>Nueva Plantilla
        </button>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="templates-container">
        <!-- Templates loaded here -->
      </div>
    </div>

    <!-- Products Section -->
    <div id="products-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Productos</h1>
          <p class="text-gray-600 mt-1">Gestiona tu catálogo de productos</p>
        </div>
        <button onclick="openCreateProductModal()" class="btn-primary">
          <i class="fas fa-plus mr-2"></i>Nuevo Producto
        </button>
      </div>

      <div class="content-card">
        <div class="overflow-x-auto">
          <table class="min-w-full">
            <thead>
              <tr>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Producto</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Precio</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Categoría</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Stock</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Acciones</th>
              </tr>
            </thead>
            <tbody id="products-table-body" class="divide-y divide-gray-100">
              <!-- Products loaded here -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- Users Section -->
    <div id="users-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Gestión de Usuarios</h1>
          <p class="text-gray-600 mt-1">Administra los usuarios del sistema</p>
        </div>
        <button onclick="openCreateUserModal()" class="btn-primary">
          <i class="fas fa-plus mr-2"></i>Crear Usuario
        </button>
      </div>

      <div class="content-card">
      <div class="overflow-x-auto">
        <table class="min-w-full">
          <thead>
            <tr>
              <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Usuario</th>
              <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Rol</th>
              <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Creado</th>
              <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase tracking-wider">Acciones</th>
            </tr>
          </thead>
          <tbody id="users-table-body" class="divide-y divide-gray-100">
            <!-- Users loaded here -->
          </tbody>
        </table>
      </div>
      </div>
      </div>

      <!-- Settings Section -->
      <div id="settings-section" class="section-content hidden p-8">
      <div class="page-header">
        <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Configuración</h1>
        <p class="text-gray-600 mt-1">Administra la configuración del bot y del negocio</p>
      </div>

      <div class="max-w-5xl space-y-6">
        <!-- Tabs de configuración -->
          <div class="content-card">
          <div class="border-b border-gray-200">
            <nav class="flex -mb-px">
              <button onclick="showSettingsTab('general')" id="tab-general" class="settings-tab px-6 py-4 text-sm font-semibold text-green-600 border-b-2 border-green-600">General</button>
              <button onclick="showSettingsTab('business')" id="tab-business" class="settings-tab px-6 py-4 text-sm font-medium text-gray-500 hover:text-gray-700">Negocio</button>
              <button onclick="showSettingsTab('messages')" id="tab-messages" class="settings-tab px-6 py-4 text-sm font-medium text-gray-500 hover:text-gray-700">Mensajes</button>
              <button onclick="showSettingsTab('payments')" id="tab-payments" class="settings-tab px-6 py-4 text-sm font-medium text-gray-500 hover:text-gray-700">Pagos</button>
              <button onclick="showSettingsTab('whatsapp')" id="tab-whatsapp" class="settings-tab px-6 py-4 text-sm font-medium text-gray-500 hover:text-gray-700">WhatsApp</button>
              <button onclick="showSettingsTab('security')" id="tab-security" class="settings-tab px-6 py-4 text-sm font-medium text-gray-500 hover:text-gray-700">Seguridad</button>
            </nav>
            </div>

          <!-- Tab: General -->
          <div id="settings-general" class="settings-panel p-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Nombre del Bot</label>
                <input type="text" id="bot-name-input" placeholder="MüllBot"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Emoji del Bot</label>
                <input type="text" id="bot-emoji-input" placeholder="🌱"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Delay de Respuesta (ms)</label>
                <div class="flex items-center gap-2">
                  <input type="number" id="bot-delay-input" min="0" step="1000" placeholder="10000"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
                  <span class="text-xs text-gray-500 whitespace-nowrap" id="current-delay-text"></span>
                </div>
                <p class="text-xs text-gray-500 mt-2">Tiempo de espera antes de responder (1000ms = 1s)</p>
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Modelo de IA</label>
                <select id="ai-model-input" class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
                  <option value="gemini-2.0-flash-exp">Gemini 2.0 Flash</option>
                  <option value="gpt-4o-mini">GPT-4o Mini</option>
                  <option value="gpt-4o">GPT-4o</option>
                </select>
              </div>
              <div class="md:col-span-2">
                <label class="block text-sm font-bold text-gray-700 mb-2">Prompt del Sistema (IA)</label>
                <textarea id="ai-system-prompt-input" rows="4" placeholder="Instrucciones personalizadas para la IA..."
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all"></textarea>
                <p class="text-xs text-gray-500 mt-2">Deja vacío para usar el prompt predeterminado</p>
            </div>
          </div>

            <!-- Sección Vendedor -->
            <div class="mt-8 pt-6 border-t border-gray-200">
              <h3 class="text-lg font-bold text-gray-800 mb-4">🎯 Perfil de Vendedor</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Personalidad del Vendedor</label>
                  <select id="seller-personality-input" class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
                    <option value="experto">🧠 Experto - Profesional y capacitado</option>
                    <option value="amigable">😊 Amigable - Cercano y genera confianza</option>
                    <option value="formal">👔 Formal - Corporativo y profesional</option>
                    <option value="persuasivo">💪 Persuasivo - Experto en técnicas de cierre</option>
                  </select>
                  <p class="text-xs text-gray-500 mt-2">Define el estilo de comunicación del bot</p>
            </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">¿Puede ofrecer descuentos?</label>
                  <div class="flex items-center gap-4 mt-2">
                    <label class="flex items-center gap-2 cursor-pointer">
                      <input type="radio" name="can-offer-discounts" id="discounts-yes" value="true" class="text-green-600 focus:ring-green-500">
                      <span class="text-sm">Sí</span>
                    </label>
                    <label class="flex items-center gap-2 cursor-pointer">
                      <input type="radio" name="can-offer-discounts" id="discounts-no" value="false" checked class="text-green-600 focus:ring-green-500">
                      <span class="text-sm">No</span>
                    </label>
                  </div>
                </div>
                <div id="discount-options" class="hidden">
                  <label class="block text-sm font-bold text-gray-700 mb-2">Descuento Máximo (%)</label>
                  <input type="number" id="max-discount-input" min="1" max="50" value="10"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
                  <p class="text-xs text-gray-500 mt-2">Porcentaje máximo que el bot puede ofrecer</p>
                </div>
                <div id="discount-conditions-container" class="md:col-span-2 hidden">
                  <label class="block text-sm font-bold text-gray-700 mb-2">Condiciones para Ofrecer Descuentos</label>
                  <textarea id="discount-conditions-input" rows="3" placeholder="Ej: Solo ofrecer descuentos cuando el cliente pregunte directamente por promociones..."
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all"></textarea>
                </div>
              </div>
            </div>
          </div>

          <!-- Tab: Business -->
          <div id="settings-business" class="settings-panel hidden p-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Nombre del Negocio</label>
                <input type="text" id="business-name-input" placeholder="Müllblue"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Horario de Atención</label>
                <input type="text" id="business-hours-input" placeholder="Lunes a Viernes 9am - 7pm"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div class="md:col-span-2">
                <label class="block text-sm font-bold text-gray-700 mb-2">Descripción del Negocio</label>
                <textarea id="business-description-input" rows="2" placeholder="Descripción corta del negocio..."
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all"></textarea>
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Teléfono</label>
                <input type="text" id="business-phone-input" placeholder="+52 123 456 7890"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Email</label>
                <input type="email" id="business-email-input" placeholder="contacto@ejemplo.com"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Sitio Web</label>
                <input type="url" id="business-website-input" placeholder="https://ejemplo.com"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Dirección</label>
                <input type="text" id="business-address-input" placeholder="Calle, Ciudad, País"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div class="md:col-span-2 border-t pt-4 mt-2">
                <h4 class="text-sm font-bold text-gray-700 mb-3">Redes Sociales</h4>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                  <div>
                    <label class="block text-xs font-medium text-gray-500 mb-1"><i class="fab fa-facebook text-blue-600 mr-1"></i>Facebook</label>
                    <input type="url" id="social-facebook-input" placeholder="https://facebook.com/..."
                      class="w-full border-2 border-gray-200 rounded-xl px-3 py-2 text-sm focus:border-green-500 outline-none transition-all">
                  </div>
                  <div>
                    <label class="block text-xs font-medium text-gray-500 mb-1"><i class="fab fa-instagram text-pink-600 mr-1"></i>Instagram</label>
                    <input type="url" id="social-instagram-input" placeholder="https://instagram.com/..."
                      class="w-full border-2 border-gray-200 rounded-xl px-3 py-2 text-sm focus:border-green-500 outline-none transition-all">
                  </div>
                  <div>
                    <label class="block text-xs font-medium text-gray-500 mb-1"><i class="fab fa-tiktok mr-1"></i>TikTok</label>
                    <input type="url" id="social-tiktok-input" placeholder="https://tiktok.com/..."
                      class="w-full border-2 border-gray-200 rounded-xl px-3 py-2 text-sm focus:border-green-500 outline-none transition-all">
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Tab: Messages -->
          <div id="settings-messages" class="settings-panel hidden p-6">
            <div class="space-y-6">
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Mensaje de Bienvenida</label>
                <textarea id="welcome-message-input" rows="4" placeholder="Mensaje inicial cuando alguien escribe por primera vez..."
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all font-mono text-sm"></textarea>
                <p class="text-xs text-gray-500 mt-2">Deja vacío para usar el mensaje predeterminado</p>
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Mensaje de Pausa (Atención Humana)</label>
                <textarea id="pause-message-input" rows="4" placeholder="Mensaje cuando se pausa el bot para atención humana..."
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all font-mono text-sm"></textarea>
                <p class="text-xs text-gray-500 mt-2">Usa {businessHours} para insertar el horario automáticamente</p>
              </div>
              <div class="bg-gradient-to-r from-green-50 to-emerald-50 border-2 border-green-200 rounded-xl p-4">
                <p class="text-sm text-green-800"><i class="fas fa-info-circle mr-2"></i>Para editar las respuestas del menú y otras respuestas automáticas, ve a la sección <strong>Contenido del Bot</strong> en el menú lateral.</p>
              </div>
            </div>
          </div>

          <!-- Tab: Payments -->
          <div id="settings-payments" class="settings-panel hidden p-6">
            <div class="space-y-6">
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Información Bancaria</label>
                <textarea id="bank-info-input" rows="5" placeholder="Banco: ...&#10;Cuenta: ...&#10;CLABE: ...&#10;Titular: ..."
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all font-mono text-sm"></textarea>
                <p class="text-xs text-gray-500 mt-2">Esta información se mostrará cuando pregunten por métodos de pago</p>
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Email de PayPal</label>
                <input type="email" id="paypal-email-input" placeholder="pagos@ejemplo.com"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
            </div>
          </div>

          <!-- Tab: WhatsApp -->
          <div id="settings-whatsapp" class="settings-panel hidden p-6">
            <div class="space-y-6">
              <div class="flex items-center justify-between p-6 bg-gradient-to-r from-gray-50 to-gray-100 rounded-2xl border-2 border-gray-200">
                <div>
                  <p class="font-bold text-gray-800">Estado de la conexión</p>
                  <p class="text-sm text-gray-600 mt-1" id="whatsapp-connected-status">Verificando...</p>
                </div>
                <button onclick="logoutWhatsApp()"
                  class="bg-red-500 hover:bg-red-600 text-white px-6 py-3 rounded-xl font-semibold transition-all shadow-lg hover:shadow-xl">
                  <i class="fas fa-unlink mr-2"></i>Desvincular
                </button>
              </div>
              <div id="qr-display" class="hidden p-8 bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl border-2 border-gray-200 flex flex-col items-center">
                <p class="text-sm font-bold text-gray-700 mb-6">Escanea el código QR con WhatsApp para vincular:</p>
                <div id="qr-code-container" class="bg-white p-6 rounded-2xl shadow-xl"></div>
                <p class="text-xs text-gray-500 mt-6">El código se actualiza automáticamente cada 20 segundos</p>
              </div>

              <!-- Gestión de Contactos Pausados -->
              <div class="p-6 bg-gradient-to-r from-yellow-50 to-orange-50 rounded-2xl border-2 border-yellow-300">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="font-bold text-yellow-800 flex items-center"><i class="fas fa-volume-mute mr-2"></i>Contactos Pausados</p>
                    <p class="text-sm text-yellow-700 mt-1">Despausar todos los contactos para que el bot pueda responderles</p>
            </div>
                  <button onclick="unpauseAllContacts()"
                    class="bg-yellow-500 hover:bg-yellow-600 text-white px-6 py-3 rounded-xl font-semibold transition-all shadow-lg hover:shadow-xl">
                    <i class="fas fa-play mr-2"></i>Despausar Todos
                  </button>
          </div>
            </div>
            </div>
          </div>

          <!-- Tab: Security -->
          <div id="settings-security" class="settings-panel hidden p-6">
            <div class="max-w-md space-y-5">
              <h3 class="font-bold text-lg text-gray-800">Cambiar Contraseña</h3>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Contraseña Actual</label>
                <input type="password" id="current-password-input"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Nueva Contraseña</label>
                <input type="password" id="new-password-input"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">Confirmar Nueva Contraseña</label>
                <input type="password" id="confirm-password-input"
                  class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 outline-none transition-all">
              </div>
              <button onclick="changePassword()" class="btn-primary w-full py-3">
                Actualizar Contraseña
              </button>
            </div>
          </div>

          <!-- Save Button -->
          <div class="p-6 bg-gradient-to-r from-gray-50 to-gray-100 border-t-2 border-gray-200 flex justify-end">
            <button onclick="saveBotConfig()" class="btn-primary px-8 py-3">
              <i class="fas fa-save mr-2"></i>Guardar Configuración
              </button>
            </div>
          </div>
        </div>
      </div>

    <!-- Bot Content Section -->
    <div id="bot-content-section" class="section-content hidden p-8">
      <div class="page-header flex justify-between items-center">
          <div>
          <h1 class="text-3xl font-bold bg-gradient-to-r from-green-600 to-emerald-600 bg-clip-text text-transparent">Contenido del Bot</h1>
          <p class="text-gray-600 mt-1">Edita las respuestas automáticas y mensajes del bot</p>
          </div>
        <div class="flex gap-3">
          <button onclick="initDefaultBotContent()" class="btn-secondary">
            <i class="fas fa-magic mr-2"></i>Inicializar Predeterminados
          </button>
          <button onclick="openCreateBotContentModal()" class="btn-primary">
            <i class="fas fa-plus mr-2"></i>Nuevo Contenido
            </button>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="bot-content-container">
        <div class="col-span-full text-center py-12 text-gray-400">Cargando...</div>
      </div>
    </div>
      </main>

      <!-- Modals -->

      <!-- Chat Modal -->
      <div id="chat-modal" class="fixed inset-0 z-50 hidden overflow-y-auto" aria-labelledby="modal-title" role="dialog"
        aria-modal="true">
        <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 bg-black bg-opacity-50 transition-opacity backdrop-blur-sm" aria-hidden="true"
            onclick="closeChatModal()"></div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-2xl sm:w-full h-[80vh] flex flex-col">
            <!-- Header -->
            <div class="bg-gradient-to-r from-green-500 to-emerald-500 px-6 py-4 border-b border-green-400 flex justify-between items-center">
              <div class="flex items-center">
                <div class="w-12 h-12 rounded-full bg-white flex items-center justify-center text-green-600 mr-3 shadow-lg">
                  <i class="fas fa-user text-lg"></i>
                </div>
                <div>
                  <h3 class="text-lg leading-6 font-bold text-white" id="chat-contact-name">Contacto</h3>
                  <p class="text-sm text-green-100" id="chat-contact-phone">Teléfono</p>
                </div>
              </div>
              <div class="flex items-center space-x-2">
                <button type="button" onclick="pauseBotFromChat(currentChatPhoneNumber)" id="pause-bot-btn"
                  class="bg-yellow-500 text-white hover:bg-yellow-600 px-4 py-2 rounded-xl text-sm font-semibold transition-all shadow-md">
                  <i class="fas fa-pause mr-1"></i>Pausar Bot
                </button>
                <button type="button" onclick="closeChatModal()"
                  class="text-white hover:text-green-100 focus:outline-none p-2 hover:bg-white/10 rounded-lg transition-all">
                  <span class="sr-only">Cerrar</span>
                  <i class="fas fa-times text-2xl"></i>
                </button>
              </div>
            </div>
            <!-- Messages -->
            <div id="messages-container" class="flex-1 overflow-y-auto p-6 space-y-4 bg-gradient-to-br from-gray-50 to-gray-100">
              <!-- Messages loaded here -->
            </div>
            <!-- Input -->
            <div class="bg-white px-6 py-4 border-t-2 border-gray-200 flex items-center gap-3">
              <textarea id="chat-message-input" rows="1"
                class="flex-1 border-2 border-gray-200 rounded-2xl px-4 py-3 focus:border-green-500 focus:outline-none resize-none transition-all"
                placeholder="Escribe un mensaje..."
                onkeydown="if(event.key === 'Enter' && !event.shiftKey) { event.preventDefault(); sendChatMessage(); }"></textarea>
              <button onclick="sendChatMessage()"
                class="btn-primary p-3 rounded-full w-12 h-12 flex items-center justify-center shadow-lg">
                <i class="fas fa-paper-plane"></i>
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Edit Content Modal -->
      <div id="edit-content-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50" onclick="closeEditContentModal()"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-2xl sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6" id="content-modal-title">Editar Respuesta
              </h3>
              <div class="space-y-5">
                <input type="hidden" id="content-key">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Descripción</label>
                  <input type="text" id="content-description" disabled
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 bg-gray-100 text-gray-600">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Contenido del Mensaje</label>
                  <textarea id="content-text" rows="10"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 focus:outline-none font-mono text-sm transition-all"></textarea>
                  <p class="text-xs text-gray-500 mt-2">Puedes usar formato de WhatsApp (*negrita*, _cursiva_,
                    ~tachado~, etc.)</p>
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Ruta de Imagen (Opcional)</label>
                  <input type="text" id="content-media-path"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 focus:outline-none transition-all">
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveBotContent()" class="btn-primary w-full sm:w-auto">
                Guardar Cambios
              </button>
              <button type="button" onclick="closeEditContentModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Status Change Modal -->
      <div id="status-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6">Cambiar Estado</h3>
              <div class="space-y-5">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Nuevo Estado</label>
                  <select id="status-select"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                    <option value="lead">Lead</option>
                    <option value="interested">Interesado</option>
                    <option value="info_requested">Info Solicitada</option>
                    <option value="payment_pending">Pago Pendiente</option>
                    <option value="appointment_scheduled">Cita Agendada</option>
                    <option value="appointment_confirmed">Cita Confirmada</option>
                    <option value="completed">Completado</option>
                  </select>
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Fecha (si aplica)</label>
                  <input type="datetime-local" id="appointment-date"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Notas</label>
                  <textarea id="status-notes" rows="3"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all"></textarea>
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveStatusChange()" class="btn-primary w-full sm:w-auto">
                Guardar
              </button>
              <button type="button" onclick="closeStatusModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Product Modal -->
      <div id="product-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6" id="product-modal-title">Nuevo Producto</h3>
              <input type="hidden" id="product-id">
              <div class="space-y-5">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Nombre</label>
                  <input type="text" id="product-name"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Descripción</label>
                  <textarea id="product-description" rows="3"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all"></textarea>
                </div>
                <div class="grid grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2">Precio</label>
                    <input type="number" id="product-price" step="0.01"
                      class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                  </div>
                  <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2">Categoría</label>
                    <input type="text" id="product-category"
                      class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                  </div>
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Tamaños (separados por coma)</label>
                  <input type="text" id="product-sizes" placeholder="S, M, L"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Promociones</label>
                  <input type="text" id="product-promotions"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">URL Imagen</label>
                  <input type="text" id="product-image-url"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div class="flex items-center">
                  <input type="checkbox" id="product-in-stock"
                    class="h-5 w-5 text-green-600 focus:ring-green-500 border-gray-300 rounded">
                  <label for="product-in-stock" class="ml-3 block text-sm font-medium text-gray-900">En Stock</label>
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveProduct()" class="btn-primary w-full sm:w-auto">
                Guardar
              </button>
              <button type="button" onclick="closeProductModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Template Modal -->
      <div id="template-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6" id="template-modal-title">Nueva Plantilla
              </h3>
              <div class="space-y-5">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Nombre</label>
                  <input type="text" id="template-name"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Contenido</label>
                  <textarea id="template-content" rows="5"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all"></textarea>
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveTemplate()" class="btn-primary w-full sm:w-auto">
                Guardar
              </button>
              <button type="button" onclick="closeTemplateModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Create User Modal -->
      <div id="create-user-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6">Crear Usuario</h3>
              <div class="space-y-5">
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Usuario</label>
                  <input type="text" id="create-username-input"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Contraseña</label>
                  <input type="password" id="create-password-input"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Rol</label>
                  <select id="create-role-select"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                    <option value="user">Usuario</option>
                    <option value="admin">Administrador</option>
                  </select>
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveCreateUser()" class="btn-primary w-full sm:w-auto">
                Crear
              </button>
              <button type="button" onclick="closeCreateUserModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- Bot Content Modal -->
      <div id="bot-content-modal" class="fixed inset-0 z-50 hidden overflow-y-auto">
        <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
          <div class="fixed inset-0 transition-opacity backdrop-blur-sm" aria-hidden="true">
            <div class="absolute inset-0 bg-black opacity-50" onclick="closeBotContentModal()"></div>
          </div>
          <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
          <div
            class="inline-block align-bottom bg-white rounded-3xl text-left overflow-hidden shadow-2xl transform transition-all sm:my-8 sm:align-middle sm:max-w-2xl sm:w-full">
            <div class="bg-white px-6 pt-6 pb-5 sm:p-8 sm:pb-6">
              <h3 class="text-2xl leading-6 font-bold text-gray-900 mb-6" id="bot-content-modal-title">Editar Contenido</h3>
              <div class="space-y-5">
                <div class="grid grid-cols-2 gap-4">
                  <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2">Clave (ID único)</label>
                    <input type="text" id="bot-content-key-input" placeholder="main_menu"
                      class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                    <p class="text-xs text-gray-500 mt-2">Identificador único para este contenido</p>
                  </div>
                  <div>
                    <label class="block text-sm font-bold text-gray-700 mb-2">Categoría</label>
                    <select id="bot-content-category-input"
                      class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                      <option value="quick_response">Respuesta Rápida</option>
                      <option value="command">Comando</option>
                      <option value="other">Otro</option>
                    </select>
                  </div>
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Descripción</label>
                  <input type="text" id="bot-content-description-input" placeholder="Descripción de este contenido..."
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Contenido del Mensaje</label>
                  <textarea id="bot-content-text-input" rows="10" placeholder="Escribe el mensaje aquí..."
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 focus:outline-none font-mono text-sm transition-all"></textarea>
                  <p class="text-xs text-gray-500 mt-2">Puedes usar formato de WhatsApp: *negrita*, _cursiva_, ~tachado~</p>
                </div>
                <div>
                  <label class="block text-sm font-bold text-gray-700 mb-2">Ruta de Media (opcional)</label>
                  <input type="text" id="bot-content-media-input" placeholder="public/images/ejemplo.jpg"
                    class="w-full border-2 border-gray-200 rounded-xl px-4 py-3 focus:border-green-500 transition-all">
                </div>
              </div>
            </div>
            <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 sm:px-8 sm:flex sm:flex-row-reverse gap-3">
              <button type="button" onclick="saveBotContent()" class="btn-primary w-full sm:w-auto">
                Guardar
              </button>
              <button type="button" onclick="closeBotContentModal()" class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>

      <script src="/public/js/admin.js"></script>
      <script>
        // Inline scripts for specific UI interactions
        function updateWhatsAppPreview() {
          const message = document.getElementById('campaign-message').value;
          const preview = document.querySelector('.whatsapp-message');
          if (preview) preview.textContent = message;
        }

        function insertVariable(variable) {
          const textarea = document.getElementById('campaign-message');
          const start = textarea.selectionStart;
          const end = textarea.selectionEnd;
          const text = textarea.value;
          textarea.value = text.substring(0, start) + variable + text.substring(end);
          textarea.selectionStart = textarea.selectionEnd = start + variable.length;
          textarea.focus();
          updateWhatsAppPreview();
        }

        // Initialize preview
        document.addEventListener('DOMContentLoaded', updateWhatsAppPreview);
      </script>
</body>

</html>
